run  ant -f build.xml

to run the program we have to execute the command

java -cp /path/to/jar/file .;./target/classes;.lib/* MusicMakerProject 

The is an executable jar file included in the package. 
to run that, run java -jar MainClass.jar